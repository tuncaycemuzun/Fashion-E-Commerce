import React from 'react'
import {View,Text} from 'react-native'
import { DefaultPageType } from '../types/types';

const Favorites = (favorites : DefaultPageType) => {
    return (
        <View>
            <Text>Favorites</Text>
        </View>
    );
}

export default Favorites;
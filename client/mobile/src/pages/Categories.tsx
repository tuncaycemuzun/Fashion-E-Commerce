import React from 'react'
import {View,Text} from 'react-native'
import { DefaultPageType } from '../types/types';

const Categories = (categories : DefaultPageType) => {
    return (
        <View>
            <Text>Categories</Text>
        </View>
    );
}

export default Categories;
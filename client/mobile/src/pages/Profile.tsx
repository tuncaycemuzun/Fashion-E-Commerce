import React from 'react'
import {View,Text} from 'react-native'
import { DefaultPageType } from '../types/types';

const Profile = (profile : DefaultPageType) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
}

export default Profile;
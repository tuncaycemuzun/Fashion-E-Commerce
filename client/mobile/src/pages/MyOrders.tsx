import React from 'react'
import {View,Text} from 'react-native'
import { DefaultPageType } from '../types/types';

const MyOrders = (myOrders : DefaultPageType) => {
    return (
        <View>
            <Text>My Orders</Text>
        </View>
    );
}

export default MyOrders;